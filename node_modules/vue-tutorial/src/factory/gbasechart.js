import * as DataCollection from '../../static/dataCollection'; 
export default class ChartMaker {
  constructor() {
    this.data = DataCollection.fetchData();
    this.xml = null;
  }

  initial = function(state){
    //var dataType = state.chartInfo.subChart.dataType;
    //   var genre = state.chartInfo.chart.genre;
    
    //var chartObj = GenreMaker.factory(genre);
    //this.chartObj.setData(data,genre);
   // this.data = DataManager.fetchData();
  }

  static factory(type) {
      return new ChartMaker[type]();
  }
}