import * as types from './mutation-types'
import ChartMaker from '../factory/gbasechart'
import AnyChart from '../factory/anychart'
import HighChart from '../factory/highchart'
import EChart from '../factory/echart'

export default {
  [types.SET_ENGINE] (state,type) {
    //ChartMaker.genre = type;
  },
  [types.INITIAL] (state,type) {
    // state.highchart = this.highchart;
    // state.anychart = this.anychart;
    //new AnyChart().initial();

  },
  [types.DRAW_CHART] (state) {
    var chart = ChartMaker.factory(state.engine);
    chart.initial(state);
    chart.draw(state); 
    // chart.final(state);
  },
  [types.CHANGE_PALETTE] (state) {
    var chart = ChartMaker.factory(state.engine);
    chart.updateChart(state); 
  },
  [types.SET_JSON] (state,type) {
    //    alert("aa");
    //  console.log("SET_JSON");
    // gbasechart.setJson(state);
   
  },
  [types.SET_TITLE] (state,prop) {
    // console.log("SET_TITLE");
    //  gbasechart.setTitle(prop.value);
    //  gbasechart.draw(state);
  },
  [types.SET_GENRE] (state,genre) {
    // console.log("SET_GENRE"+genre);
    // alert("SET_GENRE"+genre);
    //  gbasechart.setGenre(genre);
    //  gbasechart.draw(state);
  }
};